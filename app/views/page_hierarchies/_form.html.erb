<%= form_for(@page_hierarchy) do |f| %>
  <% if @page_hierarchy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_hierarchy.errors.count, "error") %> prohibited this page_hierarchy from being saved:</h2>

      <ul>
      <% @page_hierarchy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :page_id %><br>
    <%= f.number_field :page_id %>
  </div>
  <div class="field">
    <%= f.label :parent_id %><br>
    <%= f.number_field :parent_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
