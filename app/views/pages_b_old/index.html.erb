<%- model_class = Page -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:meta_keywords) %></th>
      <th><%= model_class.human_attribute_name(:meta_description) %></th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%= model_class.human_attribute_name(:body) %></th>
      <th><%= model_class.human_attribute_name(:slug) %></th>
      <th><%= model_class.human_attribute_name(:template_name) %></th>
      <th><%= model_class.human_attribute_name(:is_published) %></th>
      <th><%= model_class.human_attribute_name(:approved) %></th>
      <th><%= model_class.human_attribute_name(:publish_date) %></th>
      <th><%= model_class.human_attribute_name(:created_by_id) %></th>
      <th><%= model_class.human_attribute_name(:changed_by_id) %></th>
      <th><%= model_class.human_attribute_name(:approved_by_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @pages.each do |page| %>
      <tr>
        <td><%= link_to page.id, page_path(page) %></td>
        <td><%= page.meta_keywords %></td>
        <td><%= page.meta_description %></td>
        <td><%= page.title %></td>
        <td><%= page.body %></td>
        <td><%= page.slug %></td>
        <td><%= page.template_name %></td>
        <td><%= page.is_published %></td>
        <td><%= page.approved %></td>
        <td><%= page.publish_date %></td>
        <td><%= page.created_by_id %></td>
        <td><%= page.changed_by_id %></td>
        <td><%= page.approved_by_id %></td>
        <td><%=l page.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_page_path(page), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      page_path(page),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_page_path,
            :class => 'btn btn-primary' %>
